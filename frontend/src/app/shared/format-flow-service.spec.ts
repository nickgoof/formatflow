import { FormatFlowService } from './format-flow-service';

describe('FormatFlowService', () => {
  it('should create an instance', () => {
    expect(new FormatFlowService()).toBeTruthy();
  });
});
